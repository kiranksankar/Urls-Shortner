class Download < ApplicationRecord
  # def self.to_csv
#     attributes = %w[name ] # Define attributes you want to include

#     CSV.generate(headers: true) do |csv|
#       csv << attributes

#       all.each do |urls|
#         csv << attributes.map { |attr| urls.send(attr) }
#       end
#     end
#   end
end
